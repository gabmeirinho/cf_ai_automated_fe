{
  "name": "python-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "uv run pywrangler deploy --config wrangler.jsonc",
    "dev": "uv run pywrangler dev --config wrangler.jsonc",
    "start": "npm run dev",
    "sync": "uv run pywrangler sync"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
